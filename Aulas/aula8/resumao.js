/*Operadores: São recusros que nos permitem manipular váriaveis, realizar comparações e operções com seus valores;

    //Atribuição: atribuem valores;
    let x = 10 //atribui valor 10;
    let y = 5 // atribui valor 5;
    x += y //soma o valor de Y (x = x + y);
    x -= y //subtrai o valor de Y (x = x - y);
    x /= y //divide o valor de Y (x = x / y);
    x %= y //atribui o valor do resto de Y (x = x % y);
    x **= y //atribui o valor da exponenciação de Y (x = x ** y);
    // Para melhor visualização aplicar console.log(operador) e aplicar no painel de comando do VScode;

    //Comparação: Realizam comparações que retornam a resposta true ou false;
    x == y //Verifica se é igual;
    x != y //verifica se é diferente;
    x === y //verfifica se é estritamente igual;
    x !== y //verifica se é estritamente diferente;
    x > y //verifica se é maior;
    x < y //verifica se é menor;
    x >= y //verifica se é maior e igual;
    x <=y //verifica se é menor e igual;
    // Para melhor visualização aplicar console.log(operador) e aplicar no painel de comando do VScode;

    //Aritméricos: Realizam operações como soma, subtração;
    x + y // soma valores de x e y;
    x / y //aplica divisão em valores de x e y;
    x * y //aplica multiplicação em valores de x e y;
    x - y //aplica subtração em valores de x e y;
    x ++ //incrementa 1 ao valor;
    x-- // decréscimo de 1 ao valor;
    x % y //resto da divisão de dois valores;
    x ** y //exponenciação do valor;
    // Para melhor visualização aplicar console.log(operador) e aplicar no painel de comando do VScode;

    //Lógicos: Verificam comparações e retornam true ou false;
    true && true //True => Ambas as condições precisam ser verdadeiras;
    true || false //True ==> Uma ou ambas condições precisam ser verdadeiras
    !false //True => Nega a condição atual, ou seja vira false;
    // Para melhor visualização aplicar console.log(operador) e aplicar no painel de comando do VScode;

    //Concatenação: Concatena textos isso é, os junta em apenas uma parte
    let nome = "Cristopher"
    let sobrenome = "Gonçalves"
    let nomeCompleto = nome + " " + sobrenome //Cristopher Gonçalves*/

/*Exemplos de uso do return 
    function somar (a,b){
        return a + b;
    }
    somar (10,5);
    console.log(somar(10,5));

    function saudacoes (nome, sobrenome){
    return 'Olá ' + nome + ' ' + sobrenome;
}
    saudacoes ('Gustavo', 'Brock');
    console.log(saudacoes('Gustavo', 'Brock'));

    let nome = "Gustavo"
    let sobrenome = "Brock"
    function imc (altura, peso){
        return peso/ (altura * altura);
    }
    imc(1.8, 86)
    console.log("o imc de " + nome + sobrenome + " é " + imc(1.8, 86))*/

